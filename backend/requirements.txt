# FastAPI and Web Server Dependencies
fastapi==0.104.1          # Web framework - creates our API endpoints
uvicorn==0.24.0           # ASGI server - runs our FastAPI app
python-multipart==0.0.6  # Handles file uploads

# Database Dependencies
psycopg2-binary==2.9.9    # PostgreSQL database connection
asyncpg==0.30.0           # Async PostgreSQL driver for SQLAlchemy
sqlalchemy==2.0.23        # Database ORM (Object Relational Mapping)
alembic==1.12.1           # Database migrations (schema changes)

# AI/ML Dependencies
openai==1.3.8             # OpenAI API for chat completions
anthropic==0.39.0         # Anthropic Claude API for chat completions
google-generativeai==0.8.5 # Google Gemini AI for chat completions (fallback provider)
groq==0.11.0              # Groq LPU API - primary zero-cost completion provider (Llama 3.3 70B free tier)
huggingface-hub==0.26.2   # HF Inference API client for LLM-as-Judge (free tier)
sentence-transformers==2.2.2  # Local embedding models
langchain==0.1.0          # RAG framework and AI orchestration
pypdf2==3.0.1             # PDF text extraction

# Caching and Performance
redis==5.0.1              # Redis client for caching
python-jose==3.3.0        # JWT token handling for authentication

# Development and Utilities
python-dotenv==1.0.0      # Environment variable management
pydantic==2.12.0          # Data validation and parsing
pydantic-settings==2.11.0 # Settings management with Pydantic
typing-extensions==4.15.0 # Extended type hints

# Hackathon: dataset curation + token accounting + accuracy eval
wikipedia-api==0.7.1      # Fetch Wikipedia articles for the AI/ML history corpus
tiktoken==0.8.0           # Token counting (used to verify >=2M token budget)
bert-score==0.3.13        # BERTScore F1 for semantic-similarity accuracy metric